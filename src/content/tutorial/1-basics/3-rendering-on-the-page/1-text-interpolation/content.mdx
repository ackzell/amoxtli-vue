---
type: lesson
title: Text Interpolation
slug: text-interpolation
---
import { ToggleLessonStateButton } from '@components/SolveLessonButton'
import { AvTooltipContent, AvTooltipTrigger } from '@components/AvTooltip'

# Text Interpolation

> Text inter... what?

Just a fancy way to describe that you can use a Mustache syntax (`{{ }}`) to display dynamic content in your templates.

For example, that `message` variable we have been using in all the previous lessons, remember?

```vue showLineNumbers=false /{{/ /}}/
<h1>{{ message }}</h1>
```

But there is more you can do within those curly braces. You can run JavaScript expressions too, including method calls, arithmetic operations, and even <AvTooltipTrigger id="globals">some globals are available</AvTooltipTrigger> like `Math` or `Date`.

```vue  /{{.+}}/ {"We're familiar":2-3} {"Arithmetic expressions":9-10} {"Some globals are available":12-14} {"Conditional rendering using ternaries":17-22}
<template>

  <h1>{{ message }}</h1>

  <h1>{{ message.toUpperCase() }}</h1>

  Version is: <code>{{ version }}</code>


  <p>The answer is {{ 40 + 2 }}</p>


  <p>Random number: {{ Math.random() }}</p>
  <p>Current date: {{ new Date().toLocaleDateString() }}</p>

  <p>

    {{ 
      version.startsWith('3.')
        ? 'Using the latest Vue!'
        : 'This version is no longer officially supported'
    }}
  </p>
</template>
```

:::warn
Keep in mind that [expressions **are not** statements](https://www.joshwcomeau.com/javascript/statements-vs-expressions/).
ie. _Inside a mustache_ you can't create variables (`const message = 'Hello!'`), or use control flow statements like `if`, `for`, etc. 

Watch the compiler erroring out: <ToggleLessonStateButton client:only="react" solveStr='Try it' />
:::

<AvTooltipContent id="globals">
:::info
From the [source code](https://github.com/vuejs/core/blob/main/packages/shared/src/globalsAllowList.ts#L3) of Vue.js:
```ts
const GLOBALS_ALLOWED =
  'Infinity,undefined,NaN,isFinite,isNaN,parseFloat,parseInt,decodeURI,' +
  'decodeURIComponent,encodeURI,encodeURIComponent,Math,Number,Date,Array,' +
  'Object,Boolean,String,RegExp,Map,Set,JSON,Intl,BigInt,console,Error,Symbol'
```
:::
</AvTooltipContent>

```vue twoslash
// @errors: 2339 1005 1128

<script setup>
import { ref, version } from 'vue';
const message = ref('Interpolation');
const ok = true;

console.log('version:', version);
</script>

<template>
  
  {{ if (ok) { return message } }}
</template>

```