---
type: lesson
title: reactive()
slug: reactive
---

# reactive()

There is another way to _declare reactive state_ (or mark your variables as we called it earlier). And yes, you figured that one out: via `reactive()`